[package]
name = "sre-runtime"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Runtime provider interfaces, registry, events, and deterministic fixtures for SRE"
publish = false

[dependencies]
sre-core = { path = "../ftep-core" }
serde.workspace = true
