# FTEP treaty package

This directory contains the immutable, machine-readable treaty documents shared by the launcher, website, and control plane.

Published treaty files are append-only. A material text change requires a new versioned file and a new acceptance; existing accepted bytes must never be rewritten.
