import { InfoCard, InfoPage } from "@/app/_components/info-page";
export const metadata = { title: "Security" };
export default function SecurityPage() { return <InfoPage eyebrow="Cooperative security boundary" title="Security" intro="FTEP authorization is cryptographic but deliberately non-invasive."><InfoCard title="Signed leases"><p>Short-lived Ed25519 leases are verified locally against a public key. Server private keys never ship in SRE.</p></InfoCard><InfoCard title="Signed updates"><p>Release metadata, approved GitHub HTTPS hosts, file size, and SHA-256 are verified before an update becomes installable.</p></InfoCard><InfoCard title="Direct processes"><p>Adapters construct arguments through process APIs. They do not use shell command concatenation.</p></InfoCard></InfoPage>; }
