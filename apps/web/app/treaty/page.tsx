import accord from "../../../../packages/treaty/FICSIT-ACCORD-0001.v3.json";
export const metadata = { title: "The Accord" };
export default function TreatyPage() { return <section className="shell section"><span className="eyebrow">Version {accord.version}</span><h2>{accord.title}</h2><p className="lede">A cooperative, non-destructive policy with real-life and human-health limits built in.</p><div className="panel"><div className="metric"><span>Qualifying session</span><strong>{accord.schedule.qualifyingMinutes}+ minutes</strong></div><div className="metric"><span>Normal expected range</span><strong>{accord.schedule.expectedMinMinutes}–{accord.schedule.expectedMaxMinutes} minutes</strong></div><div className="metric"><span>Daily treaty recognition cap</span><strong>{accord.schedule.dailyRecognitionCapMinutes / 60} hours</strong></div></div><div className="grid section">{accord.articles.map((article) => <article className="card" key={article.number}><span className="eyebrow">Article {article.number}</span><h3>{article.title}</h3><p>{article.summary}</p></article>)}</div></section>; }
