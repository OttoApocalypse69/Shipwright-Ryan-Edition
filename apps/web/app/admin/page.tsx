import { AdminDataPanel } from "./admin-data-panel";
const controls = ["View users and devices", "View library and sessions", "Record Satisfactory compliance", "Suspend or restore future launch eligibility", "Review achievement state", "Mark compatibility and runtime versions", "Record scope requests", "Review immutable audit history"];
export default function AdminPage() { return <><div className="grid">{controls.map((control) => <article className="card" key={control}><h3>{control}</h3><p>Role-checked, parameterized control-plane operations with an audit record.</p></article>)}</div><AdminDataPanel section="users" /></>; }
