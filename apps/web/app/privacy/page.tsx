import { InfoCard, InfoPage } from "@/app/_components/info-page";
export const metadata = { title: "Privacy" };
export default function PrivacyPage() { return <InfoPage eyebrow="Data minimization" title="Privacy" intro="SRE and FTEP collect the minimum state needed for account authorization, compatibility, and optional synchronization."><InfoCard title="Never collected"><p>Game binaries, save contents, hardware fingerprints, unrelated processes, and diagnostic raw paths.</p></InfoCard><InfoCard title="Local by default"><p>Install paths, device private keys, cached leases, achievement writes, and diagnostics remain on the device.</p></InfoCard><InfoCard title="User control"><p>Telemetry is off unless explicitly enabled. Diagnostic exports are sanitized before sharing.</p></InfoCard></InfoPage>; }
