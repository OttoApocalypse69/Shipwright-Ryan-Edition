{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions required by the FTEP onboarding launcher.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
